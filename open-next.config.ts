import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Default Cloudflare adapter config, same as the other menuviz workers.
// See https://opennext.js.org/cloudflare
export default defineCloudflareConfig();
